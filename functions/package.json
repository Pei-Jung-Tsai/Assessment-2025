{
  "name": "functions",
  "private": true,
  "description": "Cloud Functions for Firebase",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:sendWelcomeEmail",
    "logs": "firebase functions:log --only sendWelcomeEmail"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.0",
    "cors": "^2.8.5",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.5.0"
  }
}
